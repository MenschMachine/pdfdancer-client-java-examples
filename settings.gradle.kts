rootProject.name = "pdfdancer-client-java-examples"

// Configure toolchain management to download JDKs if not found locally
plugins {
    id("org.gradle.toolchains.foojay-resolver-convention") version "1.0.0"
}
