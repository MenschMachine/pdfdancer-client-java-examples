# Enable Gradle to auto-detect installed JDKs (including those installed by GitHub Actions)
org.gradle.java.installations.auto-detect=true

# Enable auto-provisioning via Foojay resolver (configured in settings.gradle.kts)
# This allows Gradle to download JDKs if not found locally
org.gradle.java.installations.auto-download=true
